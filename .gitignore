# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.Python

# Environments
.venv/
venv/
env/
.python-version

# Secrets and local config
.env
.env.*
!.env.example
*.key
credentials.json

# uv
uv.lock

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.coverage
htmlcov/
coverage.xml

# Materialized artifacts — rebuildable from a pinned revision, tracked by hash
# in data/frozen/*.meta.json rather than by content in git.
data/frozen/

# Local model / retrieval caches — rebuildable, not evidence
.cache/
data/cache/
indexes/
indexes/
*.faiss
*.duckdb
*.sqlite3

# NOTE: results/runs/ is deliberately NOT ignored.
# Run artifacts (config.yaml, metrics.json, predictions.jsonl, env.json,
# stdout.log) are the source of truth for every number in docs/. See CLAUDE.md section 2.

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
